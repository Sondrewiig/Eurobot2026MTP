

-------------------------------------------------------------------------------
  # Run bot system

chmod +x ~/sondre_bot_gz/run_bot.sh

~/sondre_bot_gz/run_bot.sh


-------------------------------------------------------------------------------
  # Calibrate camera

  # to make it executable (after making changes to the file)

chmod +x ~/sondre_bot_gz/calibrate_camera.sh

  # run it

~/sondre_bot_gz/calibrate_camera.sh


  # or with the checkerboard size:

~/sondre_bot_gz/calibrate_camera.sh 8x6 0.024

  # where "8x6" is the grid size and "0.024" is the size of one square in meters (24mm)

-------------------------------------------------------------------------------
# Get serial data from OpenCR board


python3 -m serial.tools.miniterm /dev/ttyACM0 115200



-------------------------------------------------------------------------------
  # Source ROS

source /opt/ros/jazzy/setup.bash
source ~/sondre_bot_gz/install/setup.bash


-------------------------------------------------------------------------------
  # Update github repo

cd ~/sondre_ws

git status
git add .
git commit -m "Update Eurobot2026MTP project"
git branch -M main

git remote remove origin 2>/dev/null
git remote add origin git@github.com:SondrewiigEurobot2026MTP.git

git pull origin main --allow-unrelated-histories
git push -u origin main

-------------------------------------------------------------------------------

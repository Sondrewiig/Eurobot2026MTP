-------------------------------------------------------------------------------
  # Check input voltage

vcgencmd pmic_read_adc EXT5V_V

-------------------------------------------------------------------------------
  # Monitor CPU temp

watch -n 1 'awk "{printf \"CPU temp: %.1f°C\n\", \$1/1000}" /sys/class/thermal/thermal_zone0/temp'

-------------------------------------------------------------------------------
  # Check for throttle

sudo watch -n 1 'vcgencmd measure_temp; vcgencmd get_throttled'

-------------------------------------------------------------------------------
  # Monitor ALL

watch -n 0.5 'vcgencmd pmic_read_adc EXT5V_V; vcgencmd get_throttled; vcgencmd measure_temp'


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------

  # Run bot system


chmod +x ~/Eurobot2026MTP/run_bot.sh

~/Eurobot2026MTP/run_bot.sh

-------------------------------------------------------------------------------
  # Calibrate camera

  # to make it executable (after making changes to the file)

chmod +x ~/Eurobot2026MTP/calibrate_camera.sh

  # run it

~/Eurobot2026MTP/calibrate_camera.sh


  # or with the checkerboard size:

~/Eurobot2026MTP/calibrate_camera.sh 8x6 0.024

  # where "8x6" is the grid size and "0.024" is the size of one square in meters (24mm)

-------------------------------------------------------------------------------
# Get serial data from OpenCR board


python3 -m serial.tools.miniterm /dev/ttyACM0 115200



-------------------------------------------------------------------------------
  # Source ROS

source /opt/ros/jazzy/setup.bash
source ~/Eurobot2026MTP/install/setup.bash


-------------------------------------------------------------------------------
  # Update github repo

cd ~/Eurobot2026MTP

git status
git add .
git commit -m "Update Eurobot2026MTP project"
git branch -M main

git remote remove origin 2>/dev/null
git remote add origin git@github.com:Sondrewiig/Eurobot2026MTP.git

git pull origin main --allow-unrelated-histories
git push -u origin main

-------------------------------------------------------------------------------
